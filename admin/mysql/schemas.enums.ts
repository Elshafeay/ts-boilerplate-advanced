export enum Tables {
  User = 'users',
}

export enum Timestamps {
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',
}

export enum User {
  id = 'id',
  firstname = 'firstname',
  lastname = 'lastname',
  email = 'email',
  password = 'password',
  phoneNumber = 'phoneNumber',
}